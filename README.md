# בחירת מוצרים להזמנה - שסטוביץ

אפליקציית דפדפן לבחירת מוצרים מקטלוגי שסטוביץ והפקת הזמנה מוכנה לוואטסאפ
(שם מוצר + ברקוד + כמות).

**קישור:** https://linurbi.github.io/sch/

## שימוש

1. חיפוש לפי שם מוצר, ברקוד או מק"ט, או סינון לפי מותג.
2. לחיצה על מוצר מוסיפה אותו להזמנה; `+` / `−` לשינוי כמות.
3. פתיחת **ההזמנה** ואז:
   - **העתקה לוואטסאפ** - שם, ברקוד או מק"ט, וכמות לכל מוצר
   - **העתקה לאקסל** - `מוצר / ברקוד או מק"ט / כמות`
   - **הורדת קובץ** - אותו טקסט כקובץ `.txt`

ההזמנה נשמרת בדפדפן (`localStorage`), כך שאפשר לחזור אליה.

## מקור הנתונים

הברקוד נקרא ישירות מהברקוד המודפס בקטלוג. בקטלוג יש שני סוגי ברקודים -
ברקוד וקטורי חד, וברקוד בתמונה ברזולוציה נמוכה - ולכן הקריאה משלבת כמה
שיטות פענוח ומצליבה ביניהן. כל תוצאה נבדקת מול ספרת הביקורת, ואם שתי
שיטות חולקות על הערך המוצר לא מקבל ברקוד. במקרים כאלה מוצג המק"ט של
שסטוביץ עם תווית ברורה של `מק"ט`.

## עדכון הקטלוג

יש לשמור את קובצי המקור בשם `data/pdf/sch2025.pdf` ו-`data/pdf/sch2026.pdf`.
הייבוא קורא קודם את כל מוצרי 2025, ואז מוסיף רק מק"טים חדשים מ-2026. שכבת
הטקסט והמיקומים ב-PDF מבטיחים שכל שם משויך לתמונה הנכונה.

```powershell
py -3 -m pip install -r scripts/requirements.txt
py -3 scripts/import_pdfs.py
```

התוצאה נכתבת ל-[`data/products.json`](data/products.json) ולתמונות ב-`data/pdf-images/`.

## הרצה מקומית

```powershell
py -3 -m http.server 5173
```

ואז http://127.0.0.1:5173

## מבנה

| קובץ | תפקיד |
| --- | --- |
| [`index.html`](index.html) | מסך הבחירה |
| [`app.js`](app.js) | חיפוש, סינון, עגלה ופורמט הפלט |
| [`styles.css`](styles.css) | עיצוב RTL |
| [`scripts/import_pdfs.py`](scripts/import_pdfs.py) | ייבוא מוצרים משני קובצי ה-PDF |
| [`scripts/barcodes.py`](scripts/barcodes.py) | קריאת הברקוד המודפס בכרטיס המוצר |
| [`data/products.json`](data/products.json) | רשימת המוצרים |
